# Blue Protocol障害

バンダイナムコは、そのIDの基幹システムにAWS Lambdaを採用している。  
AWS Lambdaは、そのService Quotaでは上限が1000件のリクエストとなっている。

上限に達すると、それ以上の関数呼び出しは制限（スロットリング）される。  
スロットリングを回避するためには、以下の方法がある

- リトライ処理を組み込む
  - 同期処理の場合、スロットリングは発生でLambda関数は429を返す
